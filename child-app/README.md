# child-app/ — placeholder (name provisional until Phase 5)

The child's daily session experience — child picker with PIN, the activity flow
(`letter-tap`, `letter-trace`, `count-tap`), cached audio, the offline activity pack,
rewards (Phase 7; `docs/deliverables/srs.md` UC-03…UC-06) — will live here once Phase 5's
ADRs choose the client platform.

Until then this folder holds only this note; see `core/README.md` for why the placeholder
file exists at all.
