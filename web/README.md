# web/ — Parent and operator surfaces (placeholder)

**Provisional name. Nothing here is built until Phase 5's decision records exist.**

Two surfaces, one folder for now:

- **Parent web** — onboarding with two separate consents, the dashboard (mastery map with
  harakat progress, sessions, minutes, the current knot), "why this session today" (the
  scheduler's reasons rendered), the weekly report with its screen-free activity, the parent
  assistant (UC-11: grounded answers with sources, adult-facing only), privacy, export and delete.
- **Operator web** — curriculum and template editors with validation on save, the content
  review queue, eval results, usage and caps, validator rejection rates.

Built in **Phase 9**, in the stack chosen at E5. Requirements: `srs.md` — `FR-RPT`, `FR-LRN-008`,
`FR-CUR-008`, `NFR-I18N` (Arabic and English from the first screen, right-to-left).

If the surfaces split into two projects when the stack is chosen, this README moves with them.
